export class ButtonOne {
  text: ResourceStr;

  constructor(text: ResourceStr) {
    this.text = text;
  }
}

export class ButtonTwo {
  text: ResourceStr;

  constructor( text: ResourceStr) {
    this.text = text;
  }
}

export class ButtonThree {
  text: ResourceStr;

  constructor( text: ResourceStr) {
    this.text = text;
  }
}